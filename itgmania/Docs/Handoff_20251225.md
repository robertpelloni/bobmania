# Handoff Documentation - December 25, 2025

## Session Summary
- **Merged Feature Branch**: 
    - Merged `fix-desync-bug` into `beta`.
    - Merged `linuxpacdrive` into `beta` (resolved conflicts in CMake files).
- **Bug Fixes**:
    - Implemented `MusicWheel::SetSongs` to fix compilation of `ScreenManageHiddenSongs`.
    - Fixed VBR MP3 seeking accuracy in `RageSoundReader_MP3` by respecting `AccurateSync` and optimizing seek table usage.
- **Submodules**: Updated all submodules to their latest checked-out versions.
- **Versioning**:
    - Created `VERSION.md` with version `1.2.3`.
    - Updated `CMake/SMDefs.cmake` to read version from `VERSION.md`.
    - Created `CHANGELOG.md` to track changes.
- **Documentation**:
    - Created `Docs/ProjectDashboard.md` listing submodules and directory structure.
    - Created `LLM_INSTRUCTIONS.md` for AI agents.
    - Created `CLAUDE.md`, `AGENTS.md`, `GEMINI.md`, `GPT.md`, `copilot-instructions.md` referencing the universal instructions.

## Current State
- **Branch**: `beta`
- **Version**: 1.2.3
- **Submodules**: Up to date.
- **Upstream**: Synced with `upstream/beta`.

## Next Steps
- **Refactoring**: The `no-global.h` branch has significant conflicts but contains valuable cleanup (removing `StdString.h`). It needs a careful manual merge or rebase.
- **Formatting**: The `style-guide` branch is a massive formatting change. Merging it will cause conflicts. Consider running clang-format on the codebase incrementally instead.
- **Testing**: Verify `ScreenManageHiddenSongs` works in-game. Verify MP3 seeking in the editor.
- **Roadmap**: Check `Docs/KnownIssues.txt` for more bugs to fix.

## Memories / Context
- The project uses CMake for build configuration.
- Versioning was previously hardcoded in `CMake/SMDefs.cmake`.
- `fix-desync-bug` involved changes to `RageSound` and related classes.
- `linuxpacdrive` added `LightsDriver_LinuxPacDrive` and `USBDriver_Impl_Libusb`.
- `ScreenManageHiddenSongs` uses `MusicWheel` in a non-standard way, requiring `SetSongs`.
- `RageSoundReader_MP3` now uses `mad->tocmap` for faster seeking when `AccurateSync` is enabled.
